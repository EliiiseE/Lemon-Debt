<%= render 'header',
            image: image_tag('money-mouth-face.png'),
            small: "Enter your ",
            large: "New Expense!" %>

<h1>New Expense</h1>

<%= render 'form', expense: @expense %>

<%= link_to 'Back', expenses_path %>

<%= render 'header',
            image: image_tag(@expense.category.image),
            small: "Your",
            large: "Expense!" %>

<h1>Editing Expense</h1>

<%= render 'form', expense: @expense %>

<%= link_to 'Destroy', @expense, method: :delete, data: { confirm: 'Are you sure?' } %>
